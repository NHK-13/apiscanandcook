{% extends 'base.html.twig' %}

{% block title %}AllergeneRecette index{% endblock %}

{% block body %}
    <h1>AllergeneRecette index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomAllergene</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for allergene_recette in allergene_recettes %}
            <tr>
                <td>{{ allergene_recette.id }}</td>
                <td>{{ allergene_recette.nomAllergene }}</td>
                <td>
                    <a href="{{ path('allergene_recette_show', {'id': allergene_recette.id}) }}">show</a>
                    <a href="{{ path('allergene_recette_edit', {'id': allergene_recette.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('allergene_recette_new') }}">Create new</a>
{% endblock %}
